(defclass test-population-generators (test-base-model) ())


#|
(defmethod test-fixed-solutions-generation ((o test-population-generators))
  "Verifies basic asserts for a fixed solution population generation."
  nil)

(defmethod test-ramped-half-and-half-generation ((o test-population-generators))
  "Verifies basic asserts for a ramped half and half population generation."
  nil)

(defmethod test-random-trees-generation ((o test-population-generators))
  "Verifies basic asserts for a random trees solution population generation."
  nil)
|#